spring:
  application:
    name: quantum-gateway
  #profiles默认为dev
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 10.243.49.127:8848
      username: nacos
      password: mynacos2025
      discovery:
        namespace: Quantum
        group: ${spring.profiles.active}
      config:
        server-addr: 10.243.49.127:8848
        password: mynacos2025
        namespace: Quantum
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
  config:
    import:
      - nacos:${spring.application.name}.yaml?group=${spring.profiles.active}
      - nacos:quantum-redis.yaml?group=${spring.profiles.active}